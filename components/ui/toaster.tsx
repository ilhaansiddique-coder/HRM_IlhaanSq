// Note: Toast notifications are handled by Sonner via the ToasterProvider
// in app/layout.tsx. This file is kept for API compatibility with code that
// may import from @/components/ui/toaster, but Sonner handles all toast rendering.

export function Toaster() {
  return null;
}
